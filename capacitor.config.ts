import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.voltadigital.valorstuff',
  appName: 'ValorStuff',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
